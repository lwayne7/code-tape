---
name: progress-reporter
description: "读取 code-tape 项目 progress.md，结合项目时间规划、技术模块拆解和工作流程，将 GitHub ID 映射为同学姓名，生成带上下文的进展报告并发送到飞书群聊。"
---

# Progress Reporter

将 `docs/progress.md` 中的团队进展数据转换为带项目上下文的中文报告，发送到飞书群聊。

## 触发词

"发送进展"、"进度报告"、"群发进度"、"send progress"、"发到群聊"、"汇报进展"

## 工作流程

### 1. 读取进展数据

读取以下文件（均相对于项目根目录 `Desktop/code-tape`）：
- `docs/progress.md` — 积分与任务数据（自动生成）
- `docs/项目时间规划.md` — 阶段划分与里程碑
- `docs/技术模块拆解.md` — 技术模块与优先级
- `references/name-mapping.md` — GitHub ID 映射表

### 2. 映射 GitHub ID → 姓名

使用 `references/name-mapping.md` 中的映射表，将 GitHub 用户名替换为中文姓名。未在映射表中的 GitHub ID 保持原样。

### 3. 判断当前阶段

根据 `docs/项目时间规划.md` 的时间规划和当前日期，判断团队处于哪个阶段：

| 阶段 | 时间 | 关键词 |
| --- | --- | --- |
| 阶段一：技术方案与高风险 PoC | 5/22 - 5/24 | 技术方案、事件模型、录制包格式、关键 PoC |
| 阶段二：P0 录制链路 | 5/25 - 5/28 | 编辑器、操作录制、音视频录制、录制总控 |
| 阶段三：P0 回放链路 | 5/29 - 6/1 | 回放调度、倍速、seek、同步展示 |
| 阶段四：加分项最小展示 | 6/2 | 多语言、AI 字幕、WebRTC |
| 阶段五：联调冻结与 Demo | 6/3 | 验收、演示、兜底 |

同时识别最近的里程碑检查点：
- 5/24：技术方案冻结
- 5/28：录制链路打通
- 6/1：回放链路打通
- 6/2：加分项最小展示
- 6/3：Demo 交付（DDL）

### 4. 识别当前任务对应的技术模块

根据 Issue 标题中的关键词，匹配 `docs/技术模块拆解.md` 中的模块：

- 含"编辑器"/"Monaco"/"CodeMirror"/"语法" → 代码编辑器模块（P0）
- 含"录制"/"事件"/"操作"/"鼠标"/"快捷键"/"光标"/"选区" → 操作录制模块（P0）
- 含"音频"/"麦克风"/"摄像头"/"Media" → 音视频录制模块（P0）
- 含"总控"/"状态机"/"录制包" → 录制总控模块（P0）
- 含"回放"/"播放"/"seek"/"倍速" → 回放模块（P0）
- 含"UI"/"主题"/"布局" → UI 与交互模块（P1）
- 含"云端"/"上传"/"列表" → 云端回放中心（P1）
- 含"WebRTC"/"实时"/"面试" → WebRTC 实时面试模块（P1+）
- 含"字幕"/"AI"/"Whisper" → AI 字幕模块（P1+）
- 含"调研"/"方案"/"PoC"/"选型" → 阶段一调研类任务
- 含"沙箱"/"执行"/"运行" → 代码执行/展示沙箱（P0/P1）

未匹配的 Issue 不标注模块。

### 5. 生成格式化报告

#### 报告模板

```
📊 code-tape 训练营进展报告
更新时间：{北京时间}

📅 当前阶段：{阶段名称}（{日期范围}）
🎯 近期里程碑：{最近检查点}（{日期}）

🏃 当前任务
• {姓名} — {issue 标题}
  📦 对应模块：{技术模块名称}（{优先级}）
• {姓名} — {issue 标题}
  📦 对应模块：{技术模块名称}（{优先级}）

🏆 积分总览
• {姓名}：总分 {总分}（开发 {开发分} / CR {CR分}）
• ...

📝 最近动态
• {时间} — {描述}（{涉及人员及分数变化}）

💡 计分提醒：开发同学获得任务分×75%，CR 同学获得任务分×25%。DDL 6/3，冲！
```

格式要求：
- 时间转换为北京时间（+8），格式 `MM-DD HH:mm`
- 分数只在非零时显示
- 没有当前任务时显示"暂无认领中的任务"
- 没有流水时显示"暂无最近动态"
- 未映射的 GitHub ID 原样保留
- 当前任务附带技术模块信息，帮助同学理解任务在整体架构中的位置
- 末尾加上计分规则提醒和 DDL 鼓励（根据阶段调整语气：阶段一~二"冲！"，阶段三"稳住！"，阶段四~五"冲刺收尾！"）

### 6. 发送到飞书群聊

使用 `sessions_send` 工具发送报告。需要目标群聊的 session 标识。

如果用户未指定目标群聊，询问用户：
1. 提供飞书群的 chat_id 或 session key
2. 或者让用户将 bot 添加到目标群后，通过 `sessions_list` 查找对应 session

发送时直接使用报告文本作为 message 内容，无需额外包装。

## 注意事项

- `progress.md` 由 GitHub Actions 自动生成，格式固定，无需容错处理
- 报告语言为中文
- 分数为 0 的字段在报告中省略，保持简洁
- 更新时间使用 progress.md 中的原始 UTC 时间转换
- 阶段判断和模块匹配基于关键词，若有歧义可人工确认
